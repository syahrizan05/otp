# Large binary build outputs — regenerated by running --build --save
data/graph.obj

# Large OSM source file — download separately
data/*.pbf
data/*.osm

# Temp fix workspace
data/ktmb_fix/

# macOS junk
data/.DS_Store
.DS_Store
